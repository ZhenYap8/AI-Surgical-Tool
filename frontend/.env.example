# Local development API URL (optional — defaults to http://localhost:8000)
VITE_API_URL=http://localhost:8000

# Vercel production (set in Vercel dashboard or use frontend/.env.production):
# VITE_API_URL=/api
